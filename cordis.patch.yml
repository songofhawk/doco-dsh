# doco-dsh 作为 dsh bundle 的挂载补丁。
# `dsh plugin --profile <name> add doco-dsh` 安装后，forwarder 会把本包名加进
# profile 的 dsh.profile.bundles 列表，启动时按序 apply 这个 insert 行，
# 把 doco-dsh 挂进 profile 层栈（name 从 profile 的 node_modules 解析）。
- insert:
    - id: doco-dsh
      name: 'doco-dsh'